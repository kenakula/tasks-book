import { Divider, styled, Typography } from '@mui/material';

export const CustomDivider = styled(Divider)({
  '.MuiDivider-wrapper': { opacity: 0.4 },
  marginBottom: '20px',
});

export const CustomTextElement = styled(Typography)({
  fontSize: 12,
  marginBottom: '20px',
});
